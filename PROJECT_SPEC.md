# 深海着陆器声学应答器阵列标定与下放准入闭环

## 项目目标

构建一个面向科考船甲板作业的 Go HTTP 后端，以任务航次和着陆器为聚合边界，完成阵列构型冻结、应答器身份绑定、时钟驯服、静态回环、分时测距、整数基线联合解算、异常隔离、替换重标定、双人复核及下放准入。系统采用可重放的持久化事件日志和事务快照实现崩溃恢复；核心证据由只追加的“应答器—时钟纪元—发射序号”链与“参考点—应答器—有效测线”加权约束图组成。建议划分为 domain、catalog、calibration、resources、solver、arbitration、persistence、httpapi 等至少 8 个职责明确的包，形成约 22—28 个生产 Go 文件、2000—3000 行有效生产代码，目标约 2500 行；公开测试不少于 4 个文件和 12 个确定性用例。仓库须包含 go.mod、服务入口、数据库迁移或日志格式版本，以及支持 linux/amd64 与 linux/arm64 的多阶段 Docker 构建。

## 端到端业务流程

1. 任务建立与构型冻结：创建航次—着陆器标定任务，登记安装基座、应答器清单、三维整数坐标、甲板参考点、连续声速分层、发射码、时隙、时钟源、换能器延迟、标定测线、残差阈值和任务代次；锁定时校验摘要版本、图连通性、独立约束数量、几何非退化及整数安全范围，随后生成不可变构型摘要。
2. 原子绑定与资源占用：使用操作号一次性绑定全部应答器身份，并限时租用水槽、参考时钟端口、发射通道、接收通道和标定工位；同操作号、同规范化内容的重试返回原结果，身份复用、时隙碰撞、资源竞争或同操作号内容变化使整个事务回滚。
3. 时钟驯服与静态回环：在有效租约下连接参考时钟和收发设备，记录逻辑时间、确定重试次数及原始响应；漂移在阈值内并通过回环后建立时钟纪元。重新同步、计数器回绕或器件替换必须关闭旧纪元并创建新纪元，不复制旧纪元有效证据。
4. 分时测距与证据接收：按照冻结时隙顺序分配发射序号，记录发射证据并接收可能乱序到达的回波；每个应答器在一个纪元内每个序号最多接受一次有效接收，完全重复输入幂等，内容不同的重复输入报冲突，迟到旧纪元回波只追加为无效审计证据。
5. 联合解算与异常重标定：使用公开整数定点算法完成延迟补偿、分层传播、双向距离、加权基线和闭环残差计算；异常按共享时钟、重叠时隙及约束图传播，一次性产生排序稳定的唯一重标定集合。受影响应答器或测线进入新纪元重测，旧证据保持只读；替换器件必须重新绑定并重新获取相关租约。
6. 复核与终局准入：两名不同且资格在逻辑时间上有效的人员分别对同一当前构型摘要和解算摘要复核。准入、风险隔离和取消并发提交时，由持久化单写终局屏障只接受首个合法终局；仅全部前置条件满足时签发唯一、可查询但不可覆盖的下放准入凭据。

## 核心组件与职责

1. 阵列构型与声学规则目录：管理任务构型版本、整数坐标、声速分层、时隙、测线、阈值、人员资格和规范化摘要；提供冻结前的范围、连续性、碰撞、连通性、独立约束及退化检查。
2. 标定任务及时标聚合：实现任务代次、连续阶段前缀、时钟纪元、发射序号、设备调用记录和只追加证据链；负责纪元切换、旧回波隔离及状态重放。
3. 器件身份与资源租约管理器：维护一次性器件身份占用和五类限时互斥租约，通过同一持久化事务完成批量绑定与租约获取，支持逻辑时钟续租、过期回收和幂等操作登记。
4. 回波证据及整数基线解算器：验证回波唯一性与阶段依赖，执行分层声程、延迟补偿、双向测距、加权约束图解算、残差核验、溢出检测和稳定排序，输出可复算的解算向量与摘要。
5. 重标定与终局仲裁器：计算异常影响闭包，建立唯一重标定批次，验证双人复核资格，并通过单写终局屏障裁定准入、隔离或取消，生成不可变终局记录和准入凭据。
6. Go HTTP API：提供 JSON 命令与查询接口、稳定错误封装、请求规范化、乐观版本检查和恢复健康检查；注入逻辑时钟及脚本化设备适配器，生产环境可替换为真实设备客户端。

## 领域规则与不变量

1. 阶段依赖严格构成连续前缀：构型冻结→身份绑定→时钟驯服→静态回环→分时发射/接收→联合解算→残差通过→异常重标定完成→双人复核→终局准入；越序命令只能被拒绝，不能留下阶段或证据。
2. 证据键为航次、着陆器、应答器、纪元、发射序号和测线；同一纪元内同一应答器—序号仅有一个有效接收。序号采用显式无符号上界，下一次递增将回绕时必须关闭纪元并新建纪元，禁止在同一纪元复用零号或旧号。
3. 重新同步、漂移超限或器件替换创建严格递增的新纪元。旧纪元迟到回波可作为 received_late 审计事件追加，但不得成为有效回波、更新测线、触发解算通过或推进阶段。
4. 所有长度用整数毫米、时间用整数微秒、声速用整数毫米每秒；区间统一为左闭右开，最后一层可含终点。声速层深度边界必须首尾覆盖计算区间且相邻层端点相等。所有乘加先做溢出检查，除数必须为正；整数除法采用商加“余数绝对值大于等于除数一半时远离零”的舍入规则。
5. 单层传播距离按 round(speed_mm_s × time_us / 1_000_000)；分层传播按各层时间片分别舍入后求和；补偿时间为接收时刻减发射时刻再减收发换能器延迟，必须为正；双向距离为 round(分层传播总声程 / 2)。计数器时间差在冻结的计数器模数内仅接受小于半模数的正向差，否则报歧义或要求新纪元。
6. 基线边使用正整数权重，节点由参考点和应答器组成；图必须连通，每个待解应答器需拥有足够独立的三维约束。共线、共面不足、零长度边、重复等价约束、零权重、负值、除零及任一中间乘加溢出均稳定拒绝。加权残差按测线标识排序累计，解算输出也按固定键排序。
7. 外部设备拒绝、断连、超时、校准过期、格式错误或前后响应不一致时，只追加待重试调用记录，重试逻辑时间和最大次数由构型确定；穷尽后停留在原阶段，不得合成回波、距离、解算或复核结果。租约过期后的设备结果同样无效。
8. 所有列表、证据、重标定集合和错误明细按航次、着陆器、应答器、纪元、发射序号、测线的缺省升序确定排序。幂等键同时保存规范化内容摘要；相同内容返回原响应，不同内容返回 IDEMPOTENCY_CONFLICT。写事务失败不得遗留部分绑定、租约、有效回波、解算、终局或凭据。

## 数据模型与持久化

1. MissionTask：voyage_id、lander_id、generation、phase、config_version、frozen_digest、terminal_state、created_logical_time；航次与着陆器及任务代次组成唯一键。
2. FrozenConfiguration：安装基座、参考点与三维坐标、应答器规格、声速层、发射码、时隙表、时钟源、延迟、测线计划、残差与漂移阈值、计数器模数、重试策略、复核资格快照；冻结后只读。
3. TransponderBinding 与 ResourceLease：器件序列身份、安装点、绑定代次，以及资源类型、资源标识、持有任务、lease_token、起止逻辑时间和版本；活动身份与未过期资源分别具有唯一约束。
4. ClockEpoch 与 TimestampEvidence：纪元原因、时钟源、漂移结果、起止逻辑时间，以及发射/接收/迟到/拒绝事件、序号、测线、原始时间戳、有效性、内容摘要；事件只追加，有效接收键具有唯一索引。
5. BaselineConstraint 与 SolveResult：参考点、应答器、测线、整数距离、权重、有效纪元、解算坐标、基线长度、逐线残差、闭环残差、输入摘要和算法版本；仅完整事务可发布当前结果。
6. RetryCall、RecalibrationBatch、Review 与 TerminalDecision：保存设备调用次数及下一逻辑时间、异常原因与影响闭包、复核人和资格快照、终局屏障序号及准入凭据摘要；持久化层采用 SQLite 事务与 WAL，启动时通过迁移版本、事件日志和快照摘要校验恢复。

## 公开接口

1. POST /v1/tasks 与 POST /v1/tasks/{id}/freeze：建立任务并提交完整构型；freeze 要求 expected_version，返回冻结摘要或 CONFIG_STALE、PROFILE_GAP、PROFILE_OVERLAP、SLOT_COLLISION、GRAPH_DISCONNECTED、GEOMETRY_DEGENERATE、ARITHMETIC_OVERFLOW。
2. POST /v1/tasks/{id}/bindings:acquire：携带 Idempotency-Key 原子提交身份绑定及租约需求；POST /leases/{token}:renew 以逻辑时间续租。稳定拒绝码包括 DUPLICATE_IDENTITY、RESOURCE_BUSY、LEASE_EXPIRED 和 IDEMPOTENCY_CONFLICT。
3. POST /v1/tasks/{id}/epochs、/clock:discipline 与 /loopback:confirm：创建纪元并记录脚本化设备调用结果；错误码包括 DEVICE_REJECTED、DEVICE_TIMEOUT、DEVICE_DISCONNECTED、CALIBRATION_EXPIRED、DEVICE_FORMAT_INVALID、DEVICE_RESPONSE_MISMATCH、CLOCK_DRIFT_EXCEEDED。
4. POST /v1/tasks/{id}/transmissions 与 /echoes：登记计划发射和回波。回波请求明确 epoch、transponder、sequence、line、发送及接收微秒值；返回 EPOCH_MISMATCH、SEQUENCE_DUPLICATE、SEQUENCE_CONFLICT、SEQUENCE_WRAP、STAGE_OUT_OF_ORDER 或迟到审计状态。
5. POST /v1/tasks/{id}/solve 与 /recalibrations：发布当前纪元约束解算或根据异常生成重标定集合；查询接口返回固定顺序的约束、残差、无效证据和待重试调用。
6. POST /v1/tasks/{id}/reviews 与 /terminal-decisions：登记独立复核，并竞争 admit、isolate、cancel 终局。GET /v1/tasks/{id}、/evidence、/solution、/credential 提供恢复后查询；所有错误使用 {code,message,details}，details 为稳定排序结构。

## 失败边界

1. SQLite 单事务边界覆盖构型冻结、绑定加租约、有效回波登记、解算发布、重标定批次建立及终局裁定；任一校验、唯一约束、序列化或提交失败均整体回滚。只追加设备失败和迟到回波也各自以独立短事务提交。
2. 资源租约只依据注入的逻辑时钟判断，不依赖墙钟；崩溃恢复后，过期租约可由新操作获取，未过期租约保持互斥。恢复过程校验快照摘要，不匹配时从事件日志重放并禁止终局写入直至校验完成。
3. 外部设备调用与数据库写入不组成伪分布式事务：先持久化调用意图和尝试号，再执行脚本化适配器，最后持久化原始结果；响应丢失后的重试复用调用号，前后结果不一致时标记 DEVICE_RESPONSE_MISMATCH。
4. 解算在不可变输入快照上执行，发布时再次比较任务版本、当前纪元和构型摘要；期间发生替换、续租失败或新异常时返回 CONFIG_STALE，计算结果不得成为当前结果。
5. 终局屏障以任务唯一行和比较交换版本实现。并发准入、隔离、取消只有一个合法决策提交；失败竞争者收到 TERMINAL_ALREADY_DECIDED，并读取同一已提交终局，绝不产生第二凭据。
6. API 进程取消只终止尚未提交的事务和设备等待；已经提交的证据与终局可通过幂等键查询。JSON 格式、整数范围、未知字段或内容摘要不一致在进入业务事务前稳定拒绝。

## 验收标准

1. 锁定任务时完整固定阵列几何、逐件身份、声速剖面、时钟源、时隙、测线、延迟、阈值和任务代次；陈旧版本或不一致摘要不可冻结。
2. 整数坐标、微秒时间与毫米每秒声速的公开算法可逐步复算；声速层连续无重叠，基线图连通且三维独立约束充分，并确定拒绝负值、除零、退化几何和乘加溢出。
3. 身份绑定、时钟端口及收发通道等租约在并发下原子获取；同操作同内容重试幂等，身份复用、时隙冲突、资源冲突和内容变化全部回滚。
4. 阶段只能形成连续前缀；错误纪元、重复或冲突序号、回绕歧义、越序操作、过期租约及旧纪元迟到回波均不能形成有效证据或推进标定。
5. 设备拒绝、断连、超时、校准过期、格式错误和不一致响应只产生按逻辑时间调度且次数确定的重试记录，不产生伪造回波、距离或解算结果。
6. 漂移、串扰、异常残差、声速失配或替换按共享时钟、重叠时隙与约束图生成唯一影响闭包；新纪元覆盖全部受影响对象，旧证据始终可审计且不可覆盖。
7. 仅在绑定完整、时钟稳定、测线闭合、约束图可解、残差达标、重标定完成及两名不同合格人员独立复核后签发唯一准入凭据；并发终局只产生一个裁定。
8. 项目可通过 go test ./... 和 go vet ./...，包含不少于 20 个生产 Go 文件、4 个有意义包、2000—3000 行有效生产代码及至少 4 个公开测试文件和 12 个测试；Docker 镜像可分别构建 linux/amd64 与 linux/arm64，并能在持久卷上重启恢复。

## 确定性测试场景

1. 分层传播表驱动测试：固定三层声速、边界深度和整数微秒时间片，精确断言逐层舍入、双向距离、半值远离零、边界归属、缺层、重叠、除零及乘法溢出。
2. 约束图测试：使用公开三维整数坐标与最小约束向量，断言连通解、断图、共线、共面不足、零长度边、重复等价边及残差超过阈值的确定结果和排序。
3. 租约并发测试：用同步屏障同时争用同一水槽、时钟端口和通道，精确断言只有一个事务获得全部资源，失败者无部分身份绑定或租约；推进逻辑时钟后验证过期回收。
4. 时隙与原子绑定测试：提交身份重复、交叠时隙和已占资源的批量请求，随后查询数据库确认绑定及所有租约均未遗留；相同幂等内容返回相同响应，不同内容返回固定冲突码。
5. 纪元隔离测试：完成纪元一部分测线后重新同步建立纪元二，再乱序发送纪元一回波；断言其仅成为迟到审计事件，纪元二阶段、有效接收数和解算输入不变。
6. 序号测试：在同一纪元发送完全重复回波、内容冲突回波、乱序合法回波及接近计数器模数的时间戳，断言唯一接收、稳定冲突、固定排序、回绕歧义和新纪元要求。
7. 设备故障测试：脚本化参考时钟、发射机、接收机、温盐深探头和应答器依次返回超时、拒绝、坏格式及不一致响应，推进逻辑时间验证精确重试次数，确认不存在有效回波或解算。
8. 重标定传播测试：构造共享时钟、交叠时隙和相邻基线图，注入漂移与残差异常，断言一次生成排序确定的最小影响闭包；重复命令返回同批次，替换后产生新纪元并重测所有受影响测线。
9. 阶段前缀测试：对冻结前绑定、驯服前回环、回环前发射、测线未闭合解算以及残差失败复核分别断言 STAGE_OUT_OF_ORDER，且恢复查询显示状态未推进。
10. 双人复核测试：断言同一人员重复复核、资格过期、复核旧构型摘要及仅一人复核均不能准入；两名不同合格人员对当前摘要复核后满足准入前置条件。
11. 终局竞争测试：用同步屏障并发提交准入、隔离和取消，精确断言单写屏障只保存一个终局；若准入胜出仅有一个凭据，其余请求读取相同终局并返回 TERMINAL_ALREADY_DECIDED。
12. 重启恢复测试：在冻结、有效回波和重标定批次提交后重启服务，验证事件链、租约、当前纪元、解算摘要和幂等响应一致；模拟事务提交失败，确认没有部分绑定、有效回波、解算或凭据。
13. 十个独立缺陷支线目标：从同一初始 main 分别设置并修复层边界误归属、半值舍入错误、图连通漏判、租约双授予、绑定部分提交、旧纪元回波误生效、幂等摘要漏字段、序号回绕误接受、重标定闭包漏节点和终局双凭据；每个缺陷均由独立确定性测试揭示。

## 组件追踪关系

1. 阵列构型与声学规则目录对应验收 1、2，主要由分层传播、约束图、时隙碰撞和阶段前缀测试覆盖。
2. 标定任务及时标聚合对应验收 4、5，主要由纪元隔离、序号、设备故障、阶段前缀和重启恢复测试覆盖。
3. 器件身份与资源租约管理器对应验收 3，主要由租约并发、时隙与原子绑定、幂等冲突及事务失败测试覆盖。
4. 回波证据及整数基线解算器对应验收 2、4，主要由分层传播、公开最小约束向量、乱序重复回波和残差阈值测试覆盖。
5. 重标定与终局仲裁器对应验收 6、7，主要由重标定传播、双人复核、同步屏障终局竞争和重启凭据唯一性测试覆盖。
6. Go HTTP API 对应验收 8并横跨全部业务验收，负责稳定错误码、严格 JSON、固定排序、逻辑时钟注入、脚本故障注入及持久化恢复的端到端验证。

## 独特性

该计划聚焦深海着陆器布放前的甲板声学阵列标定，将时钟纪元证据链、分层声速整数传播、加权三维基线约束图、器件替换影响闭包与单写下放终局屏障组合成一个连续工作流；其标题、对象和裁定机制均不与既有深海滑翔机首次下潜或深海采样器压力舱耐压项目重复。
